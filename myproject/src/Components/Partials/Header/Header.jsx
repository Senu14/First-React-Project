// We return our components under return (
const Header = props => {
     return (
          <header>
            <h1>{props.PageTitle}</h1>

            {/* <nav className="headerNav"> */}
                    
          </header>
     );
};

export { Header };